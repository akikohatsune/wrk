name: Build wrk

on:
  push:
  pull_request:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-ubuntu-macos:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-22.04, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: runner.os == 'Linux'
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libssl-dev git unzip perl

      - name: Build wrk
        run: make

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: wrk-${{ matrix.os }}
          path: wrk

  build-arm64:
    name: Build for ARM64 (Android/Termux/iOS)
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install Cross-Compiler and Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential gcc-aarch64-linux-gnu libssl-dev git unzip perl luajit

      - name: Build wrk for ARM64
        run: |
          make CC=aarch64-linux-gnu-gcc CROSS=aarch64-linux-gnu-

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: wrk-arm64
          path: wrk

  build-arch:
    name: Build on Arch Linux
    runs-on: ubuntu-22.04
    container:
      image: archlinux:latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          pacman -Syyu --noconfirm
          pacman -S --noconfirm base-devel openssl git unzip perl

      - name: Build wrk
        run: make

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: wrk-archlinux
          path: wrk

  build-rpm:
    name: Build RPM (Fedora)
    runs-on: ubuntu-22.04
    container:
      image: fedora:36
    steps:
      - name: Install dependencies
        run: |
          dnf install -y @development-tools openssl-devel git unzip rpm-build perl-FindBin perl-IPC-Cmd perl-File-Compare perl-File-Copy

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build wrk
        run: make

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: wrk-fedora-rpm
          path: wrk
